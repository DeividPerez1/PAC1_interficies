<UserControl x:Class="WPF_MVVM_SPA_Template.Views.Option2View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVM_SPA_Template.Views"
        mc:Ignorable="d" d:DesignWidth="640.884" d:DesignHeight="456.964">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Seleccioneu el tema de l'aplicació:" 
                       FontFamily="Segoe UI Semibold" 
                       FontSize="14" 
                       Foreground="#2C3E50" 
                   
                        Height="100" Width="220"/>

            <Button Content="Aplicar Tema" Command="{Binding AddCourseCommand}"   HorizontalAlignment="Center " 
               Style="{StaticResource ModernPrimaryButtonStyle}" Width="190" Height="50" >
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-0.212"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
            </Button>
        </StackPanel>

    </Grid>
</UserControl>
