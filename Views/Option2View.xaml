<UserControl x:Class="WPF_MVVM_SPA_Template.Views.Option2View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVM_SPA_Template.Views"
        mc:Ignorable="d" d:DesignWidth="640.884" d:DesignHeight="456.964">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <TextBlock Text="Seleccioneu el tema de l'aplicació:" 
                   FontFamily="Segoe UI Semibold" 
                   FontSize="42" 
                   Foreground="#2C3E50" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <Button Content="Afegir" Command="{Binding AddCourseCommand}" Margin="318.938,176.974,297.93,109.769"  HorizontalAlignment="Center " 
           Style="{StaticResource ModernPrimaryButtonStyle}" Width="122.274" >
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-0.212"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>

    </Grid>
</UserControl>
